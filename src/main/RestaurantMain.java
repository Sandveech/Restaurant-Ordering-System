package src.main;

import java.util.ArrayList;

import src.model.Cart;
import src.model.Employee;
import src.model.MenuItem;
import src.model.OrderItem;
import src.model.Receipt;
import src.model.Table;

public class RestaurantMain {
    public static void main(String[] args) {
        // Table table1 = new Table();

        // Employee waiter1 = new Employee("Hout", "Khongdara");

        // MenuItem item1 = new MenuItem(5, "Carbonara", "Italian cream.", "Pasta");
        // MenuItem item2 = new MenuItem(5, "Aglio e Olio", "Garlic and oil.", "Pasta");

        // // ArrayList<MenuItem> menu_list = new ArrayList<MenuItem>();
        // // menu_list.add(carbonara);
        // // menu_list.add(algio_e_olio);

        // OrderItem order1 = new OrderItem(item1, 1, 's');
        // OrderItem order2 = new OrderItem(item2, 2, 's');
        // OrderItem order3 = new OrderItem(item1, 10, 'l');

        // Cart cart = new Cart(1, 10);
        // cart.addOrder(order1).addOrder(order2).addOrder(order3);

        // //cart.printOrders();

        // Receipt receipt1 = new Receipt(table1, cart, waiter1);
        // receipt1.printDetails();
    }
}
